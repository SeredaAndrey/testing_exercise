// const { useDispatch } = require('react-redux');

const HomePage = () => {
  //   const dispatch = useDispatch();

  return (
    <section>
      <h2>Home</h2>
    </section>
  );
};

export default HomePage;
