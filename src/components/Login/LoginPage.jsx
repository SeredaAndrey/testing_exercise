// const { useDispatch } = require('react-redux');

const LoginPage = () => {
  //   const dispatch = useDispatch();

  return (
    <section>
      <h2>Login</h2>
    </section>
  );
};

export default LoginPage;
