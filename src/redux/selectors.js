export const selectIsLoggedIn = state => state.auth.isLoggedIn;
export const selectIsLoading = state => state.auth.isLoading;
export const selectIsName = state => state.auth.name;
export const selectIsAvatar = state => state.auth.avatarUrl;
export const selectIsUserId = state => state.auth.userId;
export const selectIsFollowing = state => state.auth.following;
